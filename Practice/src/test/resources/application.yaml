spring:
  datasource:
    url: jdbc:h2:mem:libdb
  sql:
    init:
      mode: always
      data-locations: data.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: false